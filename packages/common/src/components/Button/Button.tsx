import React, {FC} from 'react';
import {Text, TouchableOpacity} from 'react-native';

const Button: FC = () => {
  return (
    <TouchableOpacity>
      <Text>Button</Text>
    </TouchableOpacity>
  );
};

export default Button;
